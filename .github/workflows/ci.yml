name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  heavy-build:
    runs-on: ubuntu-latest-l
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Simulating OOM"
          exit 137