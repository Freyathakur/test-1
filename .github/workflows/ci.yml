name: CI
on:
  push:
    branches: [main, "smoke/**", "cicd-copilot/**"]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build
