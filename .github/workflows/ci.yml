name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  slow-job:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - uses: actions/checkout@v4
      - run: sleep 90