name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - uses: actions/checkout@v4
      - name: Long-running test
        run: |
          echo "starting heavy test"
          sleep 75
          echo "done"

